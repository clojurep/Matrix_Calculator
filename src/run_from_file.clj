(ns run-from-file
  (:use [Matrix-Calculator])
  
  )


;;; Change the file name if you want to
;;; load a different input text file.

;;; Your file must be in the input.txt's path.
;;; Otherwise, write the file's full path here.


;;; To execute the file, press Ctrl+Alt+S

(load-file "input.txt")
